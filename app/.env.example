# Public Flutter client configuration only.
# Copy this file to .env.dev, .env.staging, or .env.prod and replace the values.
# Never add service-role, LLM, RevenueCat REST/webhook, signing, or database secrets.

SUPABASE_URL=https://replace-with-project-url.invalid
SUPABASE_PUBLISHABLE_KEY=replace_with_public_publishable_or_anon_key
